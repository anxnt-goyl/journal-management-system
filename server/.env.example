

PORT=5000
NODE_ENV=development

# MongoDB connection string (use MongoDB Atlas in production)
MONGODB_URI=mongodb+srv://anantg:Goyal987@cluster0.ss9zsiw.mongodb.net/journal-management-system?appName=Cluster0

# Required — generate a long random string for production, e.g.:
#   node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET="hnbcfcgcgfhvfrukbyzancucdwjcbjbhjbjhjbjjjs"

JWT_EXPIRES_IN=7d

# Comma-separated list of frontend origins allowed to call this API
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Cloudinary (paper file uploads)
CLOUDINARY_CLOUD_NAME=yptoyzeg
CLOUDINARY_API_KEY=279456592136845
CLOUDINARY_API_SECRET=T23k5j8ErXlXWsEUzLfK2wT9la4

# SMTP (email notifications) — optional, mailer no-ops if unset
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
